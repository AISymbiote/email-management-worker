# Legacy reference

这里保存的是 Worker 主线之外的历史参考代码和旧文档，默认不参与当前部署。

当前主线只需要仓库根目录的 Worker 配置、`src/`、`email-management-app/` 和 `migrations/`。

## 内容说明

- `_figma_source/`：历史 React 前端实验版本
- `email-management-backend/`：历史 Flask 后端
- `outlook-manager/`：早期原型
- `docs/`：历史架构、部署和开发文档
- `docker-compose.yml`、`.env.example`：早期部署尝试配置

除非你明确要参考旧实现，否则不要修改或依赖这里的内容。
